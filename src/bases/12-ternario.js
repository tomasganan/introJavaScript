// Operador condicional ternario

// const activo = true;

/*let mensaje = '';

 if(activo){
    msg = 'Activo';
}else{
    msg = 'Inactivo'
} */

// Operador ternario. ? -> cuando se cumple. : -> Cuando no

// const msg = (activo) ? 'Activo' : 'Inactivo';

// Forma corta de hacer un IF

// const mensaje = (activo) && 'Activo'